name: hello-world
'on': push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: my-step
        run: echo "Hello World!"

      - name: my-dir
        run: pwd

      - name: my-ls
        run: ls -l
